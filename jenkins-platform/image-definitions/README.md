# image-definitions

This directory will contain source files required to build Jenkins-related container images.
Examples include Dockerfiles, build contexts, and image metadata.

## Available Image Definition

- `jenkins-controller/`: Jenkins controller container image definition.
