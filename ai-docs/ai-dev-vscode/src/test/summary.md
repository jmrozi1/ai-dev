- `ai-dev-vscode/src/test/extension.test.ts` — Defines the extension’s main unit/integration test suite covering config resolution (`readAiDevConfig`, `resolveAiDevCorePath`), batch unit-doc limits and large-run warnings, assistant input state and slash-command parsing (`parseSlashCommand`, `resolveAskCommand`), automatic chat/summary routing, terminal/report/command helpers, and summarization config workflows; read this file for expected behavior, regression coverage, and edge-case contracts of these modules.