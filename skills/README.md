# Skills

This directory contains reusable agent skills, organized by category.

Each skill belongs inside a category directory. Do not place individual skills directly under `skills/`.

`index.md` is the generated catalog used to discover and route to skills.
